--- panels/meson.build.orig	2026-09-11 00:00:00 UTC
+++ panels/meson.build
@@ -16,16 +16,14 @@ panels = [
   'printers',
   'privacy',
   'search',
-  'sharing',
   'sound',
   'system',
   'universal-access',
   'wellbeing',
-  'wwan',
 ]
 
 if host_is_linux
-  panels += ['network']
+  panels += ['network', 'sharing', 'wwan']
 endif
 
 if host_is_linux_not_s390
