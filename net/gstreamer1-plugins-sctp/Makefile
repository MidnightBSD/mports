PORTREVISION=	1
CATEGORIES=	net

COMMENT=	GStreamer sctp plugin

MASTERDIR=	${.CURDIR:H:H}/multimedia/gstreamer1-plugins

PLIST=		${.CURDIR}/pkg-plist

DIST=		bad
GST_PLUGIN=	sctp

.include "${MASTERDIR}/Makefile"
