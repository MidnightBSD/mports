PORTNAME=	syndication
DISTVERSION=	${KDE_FRAMEWORKS_VERSION}
CATEGORIES=	net kde kde-frameworks

COMMENT=	Parser library for RSS and Atom feeds
MAINTAINER=	ports@MidnightBSD.org

USES=		cmake kde:6 qt:6 tar:xz
USE_KDE=	codecs \
		ecm:build
USE_QT=		base

.include <bsd.port.mk>
