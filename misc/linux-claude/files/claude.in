#!/bin/sh
#
# Wrapper script for Claude Code CLI.
# BUN_JSC_useBBQJIT=0 is required because the BBQ JIT compiler in Bun's
# JavaScriptCore is not supported under MidnightBSD Linux emulation.
#
BUN_JSC_useBBQJIT=0
export BUN_JSC_useBBQJIT
exec %%PREFIX%%/lib/linux-claude/claude "$@"
