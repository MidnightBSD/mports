--- meson.build.orig
+++ meson.build
@@ -46 +46 @@
-gudev_req = '>= 238'
+gudev_req = '>= 232'
--- src/backends/meta-udev.c.orig
+++ src/backends/meta-udev.c
@@ -145 +145 @@ meta_is_udev_device_preferred_primary (GUdevDevice *device)
-  tags = g_udev_device_get_current_tags (device);
+  tags = g_udev_device_get_tags (device);
