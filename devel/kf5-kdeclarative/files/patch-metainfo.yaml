--- metainfo.yaml.orig	2025-09-18 14:21:45 UTC
+++ metainfo.yaml
@@ -5,6 +5,7 @@ type: functional
 platforms:
     - name: Linux
     - name: FreeBSD
+    - name: MidnightBSD
     - name: Windows
     - name: macOS
       note: The supported remote URLs depend on the set of KIO workers installed at runtime.
