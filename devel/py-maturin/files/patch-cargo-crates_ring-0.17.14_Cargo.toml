--- cargo-crates/ring-0.17.14/Cargo.toml.orig	1970-01-01 00:00:01 UTC
+++ cargo-crates/ring-0.17.14/Cargo.toml
@@ -264,7 +264,7 @@ features = [
 version = "0.2.155"
 default-features = false
 
-[target.'cfg(all(any(all(target_arch = "aarch64", target_endian = "little"), all(target_arch = "arm", target_endian = "little")), any(target_os = "android", target_os = "linux")))'.dependencies.libc]
+[target.'cfg(all(any(all(target_arch = "aarch64", target_endian = "little"), all(target_arch = "arm", target_endian = "little")), any(target_os = "android", target_os = "freebsd", target_os = "linux")))'.dependencies.libc]
 version = "0.2.148"
 default-features = false
 
