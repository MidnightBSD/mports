# $MidnightBSD$
#

    COMMENT = Monetary, financial and related applications

SUBDIR += kexchange
SUBDIR += kmymoney2
SUBDIR += libofx
SUBDIR += openhbci
SUBDIR += opentaxsolver
SUBDIR += wmstock

.include <bsd.port.subdir.mk>
