--- bin/mamprobe.orig	2012-08-01 15:38:23 UTC
+++ bin/mamprobe
@@ -187,7 +187,14 @@ echo "note generated by $0 for $cc"
 	. $makeprobe "$@"
 
 	case " $CC_DIALECT " in
-	*" -L "*)	echo "CC.L = 1" ;;
+	*" -L "*)	echo "CC.L = 1" ;;
+	*)	case $CC_HOSTTYPE in
+		midnightbsd.*)
+			# MidnightBSD supports -L even when the probe misses it.
+			echo "CC.L = 1"
+			;;
+		esac
+		;;
 	esac
 
 ) | sed \
