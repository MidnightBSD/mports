# $MidnightBSD$
#

    COMMENT = Internet Relay Chat utilities

SUBDIR += bitlbee
SUBDIR += bnc
SUBDIR += irssi
SUBDIR += irssi-scripts
SUBDIR += ngircd
SUBDIR += sirc
SUBDIR += talksoup
SUBDIR += xchat
SUBDIR += znc

.include <bsd.port.subdir.mk>
